# Django + RAG + Gemini (PythonAnywhere / generic pip)
# Free PythonAnywhere accounts: first request may be slow; ML stack needs RAM (upgrade if workers crash).

django==5.0
pandas==2.2.3
numpy==1.26.4
torch==2.6.0
sentence-transformers==5.1.0
transformers==4.52.1
accelerate==1.4.0
tqdm==4.67.1
python-dotenv>=1.0.0
google-genai
faiss-cpu
